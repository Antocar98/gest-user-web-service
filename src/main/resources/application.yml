#==========================================================
#= GestUser Web Service - Versione Base
#==========================================================

server:
  port: 8101
spring:
  application:
    name: GestUser
#==========================================================
#= PARAMETRI DB MONGODB 
#==========================================================
  data:
    mongodb:
      authentication-database: admin
      database: ms-users
      host: 172.22.0.2  # Indirizzo IP statico di MongoDB nella rete Docker
      password: 123Stella
      port: 27017       # Usa la porta interna di MongoDB, non quella esposta sull'host
      username: nicola
      auto-index-creation: true