#==========================================================
#= GestUser Web Service - Versione Base
#==========================================================

server:
  port: 8100
spring:
  application:
    name: GestUser
#==========================================================
#= PARAMETRI DB MONGODB 
#==========================================================
  data:
    mongodb:
      host: mongodb   # Usa il nome del servizio Docker invece dell'IP
      port: 27017         # Questa linea è opzionale se usi la porta di default
      database: ms-users
      authentication-database: admin
      username: antonio
      password: antonio